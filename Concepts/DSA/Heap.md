---
slug: dsa-heap
---

# Heap

## Definition

A priority queue structure (usually a binary heap) that always exposes the minimum or maximum element in O(1) and extracts it in O(log n).

## Description

Used for top-k, scheduling, and streaming medians (often with two heaps).
