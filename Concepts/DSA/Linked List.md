---
slug: dsa-linked-list
kind: schema
---

# Linked List

## Definition

A linear structure of nodes where each node stores a value and a reference to the next (and sometimes previous) node.

## Description

Supports O(1) insert/delete given a reference; random access is O(n). Patterns: reverse, merge, cycle detection, two pointers.
